import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Login({}: Props): ReactElement {
    return (
        <div>
            <p>Login</p>
        </div>
    )
}
