import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Something({}: Props): ReactElement {
    return (
        <div>
            <p>Something</p>
        </div>
    )
}
